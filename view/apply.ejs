<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Application Form</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #f3f4f6, #ffffff); /* Light gradient background */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden; /* Hide overflow for a cleaner look */
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        form {
            background: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Subtle shadow */
            max-width: 600px;
            width: 100%;
            position: relative;
            overflow: hidden; /* Ensure rounded corners are visible */
        }

        form::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #1d5aea, #154bb5);
            opacity: 0.1; /* Subtle overlay */
            z-index: 0;
        }

        div {
            position: relative;
            margin-bottom: 20px;
        }

        label {
            position: absolute;
            top: 15px;
            left: 15px;
            font-size: 14px;
            color: #888;
            transition: all 0.3s ease;
            pointer-events: none; /* Ensure clicks on label go to input */
        }

        input, textarea {
            width: 100%;
            padding: 15px;
            border: none; /* Remove borders */
            border-radius: 8px;
            background: #f9f9f9;
            color: #333;
            font-size: 16px;
            outline: none;
            box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.1); /* Subtle shadow for input focus */
            transition: all 0.3s ease;
        }

        input:focus, textarea:focus {
            box-shadow: 0 0 0 2px #1d5aea; /* Focus shadow effect */
        }

        input:focus + label, textarea:focus + label,
        input:not(:placeholder-shown) + label, textarea:not(:placeholder-shown) + label {
            top: -10px;
            left: 15px;
            font-size: 12px;
            color: #1d5aea;
        }

        button {
            background-color: #1d5aea;
            color: #fff;
            padding: 15px;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            width: 100%;
            z-index: 1; /* Ensure button is on top */
        }

        button:hover {
            background-color: #154bb5;
            transform: translateY(-2px); /* Slight lift effect */
        }

        button:active {
            transform: translateY(0); /* Press effect */
        }

        textarea {
            resize: vertical; /* Allow vertical resizing */
            min-height: 100px;
        }
    </style>
</head>
<body>
    <h1>Job Application Form</h1>
    <form action="/apply" method="post">
        <div>
            <input type="text" id="fullName" name="name" required placeholder=" ">
            <label for="fullName">Full Name</label>
        </div>

        <div>
            <input type="email" id="email" name="email" required placeholder=" ">
            <label for="email">Email</label>
        </div>

        <div>
            <input type="tel" id="phone" name="phone" required placeholder=" ">
            <label for="phone">Phone Number</label>
        </div>

        <div>
            <input type="text" id="position" name="position" value="<%= company_requirement %>" readonly placeholder=" ">
            <label for="position">Position Applied For</label>
        </div>

        <div>
            <textarea id="coverLetter" name="coverLetter" rows="4" required placeholder=" "></textarea>
            <label for="coverLetter">Cover Letter</label>
        </div>

        <div>
            <button type="submit">Submit Application</button>
        </div>
    </form>
</body>
</html>
