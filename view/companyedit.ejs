<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Company</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #1d5aea, #4b6cb7); /* Gradient background */
            color: #fff;
        }

        .bck1 {
            background: rgba(255, 255, 255, 0.1); /* Light overlay */
            backdrop-filter: blur(10px); /* Blur effect */
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .blur1 {
            background: rgba(255, 255, 255, 0.9); /* Transparent background */
            border-radius: 12px;
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.3);
            max-width: 400px;
            margin: 100px auto;
            padding: 40px;
            position: relative;
            z-index: 1;
            text-align: center;
            animation: fadeIn 2s ease-in-out; /* Fade-in animation */
        }

        .blur1 h1 {
            margin-bottom: 30px;
            color: #333;
            font-size: 2.5em;
        }

        .blur1 form {
            display: flex;
            flex-direction: column;
        }

        .blur1 form div {
            position: relative;
            margin-bottom: 20px;
        }

        .blur1 label {
            position: absolute;
            top: 50%;
            left: 15px;
            font-weight: bold;
            color: #777;
            font-size: 16px;
            transform: translateY(-50%);
            transition: 0.3s ease;
            pointer-events: none;
        }

        .blur1 input {
            width: 100%;
            padding: 15px;
            border: none; /* No border */
            border-radius: 8px;
            font-size: 16px;
            background: #f0f0f0;
            color: #333;
            box-shadow: 0 0 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
            transition: box-shadow 0.3s ease;
        }

        .blur1 input:focus {
            box-shadow: 0 0 10px rgba(29, 90, 234, 0.4); /* Focus effect */
        }

        .blur1 input:focus + label,
        .blur1 input:not(:placeholder-shown) + label {
            top: -10px;
            left: 15px;
            font-size: 12px;
            color: #1d5aea;
            font-weight: normal;
        }

        .blur1 button {
            background-color: #1d5aea;
            color: #fff;
            padding: 15px;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .blur1 button:hover {
            background-color: #154bb5;
            transform: scale(1.05); /* Slight zoom effect */
        }

        .blur1 button:active {
            transform: scale(0.95); /* Press effect */
        }

        /* Animation for background */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <div class="bck1"></div>

    <div class="blur1">
        <h1>Edit company</h1>
        <form action="/companyedit/<%= id%>" method="post">
            <div>
                <input type="text" id="companyName" name="compuny_name" value= "<%= user.compuny_name %>"required placeholder=" ">
                <label for="companyName">Company Name:</label>
            </div>
            
            <div>
                <input type="text" id="Salary" name="compuny_salary" value= "<%= user.compuny_salary %>" required placeholder=" ">
                <label for="Salary">Salary:</label>
            </div>
            
            <div>
                <input type="text" id="location" name="compuny_location" value= "<%= user.compuny_location %>" required placeholder=" ">
                <label for="location">Location:</label>
            </div>
            
            <div>
                <input type="text" id="description" name="company_requirement" value="<%= user.company_requirement %>" required placeholder=" ">
                <label for="description">Requirements:</label>
            </div>

            <div>
                <button type="submit">Add Company</button>
            </div>
        </form>
    </div>
</body>
</html>
